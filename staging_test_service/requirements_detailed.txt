# Environment for the DETAILED locust load test job only
# (schedules/locust_loadtest_detailed.yaml). Keep in sync with
# requirements.txt and add report-generation deps here.
grpcio>=1.50
locust
pydantic>=2.0
# Locust 2.x needs urllib3 2.x (create_urllib3_context). Botocore pins urllib3<2.1 on Ray images.
urllib3>=2.0.5,<2.1
aiohttp
aiohttp-retry
# Self-contained HTML charts for report_detailed.html
plotly>=5.24
# Pre-signed S3 URL for the report_detailed.html Slack thread reply.
boto3
